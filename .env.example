# ================================================================
# FICHIER .ENV D'EXEMPLE
# Copiez ce fichier en .env.local et adaptez les valeurs
# ================================================================

# ── Environnement ───────────────────────────────────────────────
APP_ENV=dev
APP_DEBUG=true
APP_SECRET=CHANGEZ_CE_SECRET_ICI

# ── Base de Données (Docker Local) ─────────────────────────────
# Pour développement local avec Docker
DATABASE_URL="mysql://symfony:symfony@mysql:3306/symfony?serverVersion=8.0"

# ── Base de Données (Production Hostinger) ─────────────────────
# Pour production, utilisez ces valeurs dans .env.local sur le serveur
# DATABASE_URL="mysql://db_user:db_password@localhost:3306/db_name?serverVersion=8.0"

# ── Mailer (Development avec Mailpit) ──────────────────────────
MAILER_DSN=smtp://mailpit:1025

# ── Mailer (Production) ────────────────────────────────────────
# MAILER_DSN=smtp://smtp.example.com:587?encryption=tls&auth_mode=login&username=&password=
